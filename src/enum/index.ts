export enum ChatTypeEnum {
    VOICE = 'voice',
    TEXT = 'text',
  }
  
  export enum MatchSessionStatusEnum {
    WAITING = 'waiting',
    CONNECTED = 'connected',
    ENDED = 'ended',
  }
  
  export enum GenderEnum {
    MALE = 'male',
    FEMALE = 'female',
    OTHER = 'other',
  }
  